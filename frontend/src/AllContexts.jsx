"use client"
import { createContext} from "react";

export const RapidQuestionContext = createContext("rapid-question-context")
export const LiveQuestionContext = createContext("live-question-context")